const ClientProjects = () => {
  return (
    <div>
      <h2>My Projects (View Only)</h2>
      <p>Project details + team count + hours + progress</p>
      <p>Option: Extend / Reduce timeline</p>
    </div>
  );
};

export default ClientProjects;